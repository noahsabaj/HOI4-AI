[project]
name = "peer"
version = "0.1.0"
description = "Run work on the second PC from any project on this one"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
peer = "peer:main"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["peer"]
